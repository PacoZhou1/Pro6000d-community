--- a/b12x/moe/fused_moe/_impl.py
+++ b/b12x/moe/fused_moe/_impl.py
@@ -2684,6 +2684,25 @@
     query: MoeDecodeQuery,
     device: DeviceIdentity | None,
 ) -> str:
+    # Bounded RTX 6000D / DS4 TP4 experiment; resolved before graph capture.
+    if (
+        os.getenv("HOMELAB_DS4_MOE_M4_PACKED") == "1"
+        and device is not None
+        and device.product_name == "nvidia rtx 6000d"
+        and device.compute_capability == (12, 0)
+        and device.sm_count == 156
+        and query.quant_mode == "w4a16"
+        and query.num_tokens == 4
+        and query.routed_rows == 24
+        and query.num_experts == 256
+        and query.hidden_size == 4096
+        and query.intermediate_size == 512
+        and query.top_k == 6
+        and query.activation == "silu"
+        and query.source_format == "fp4_e8m0_k32"
+    ):
+        logger.info("HOMELAB DS4 M4 MoE: packed route selected before capture on RTX 6000D")
+        return "packed"
     if not _w4a16_direct_routing_supported(query):
         return "packed"
     if (
